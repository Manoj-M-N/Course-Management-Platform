# Server Configuration
server.port=8080
spring.application.name=course-platform

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/course_management_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=4su21cs052
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Initialize database with data.sql
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true

# JWT Configuration
jwt.secret=gisul-course-platform-secret-key-2024-make-this-very-long-and-secure-in-production
jwt.expiration=86400000

# CORS Configuration
cors.allowed.origins=http://localhost:3000,http://localhost:5173

# Logging
logging.level.com.gisul.courseplatform=DEBUG
logging.level.org.springframework.security=DEBUG
